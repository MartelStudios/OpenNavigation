name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      # Runs the tests on the way: a red test is a red build
      - name: Build
        run: ./gradlew build

      # Only worth carrying when something broke, and then it is the whole story
      - name: Upload the test report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: build/reports/tests/test

      # So a pull request can be tried without building it locally
      - name: Upload plugin
        uses: actions/upload-artifact@v4
        with:
          name: plugin
          path: build/libs/*.jar
          if-no-files-found: error
